version: '3.8'

services:
  service_monitor:
    build: .
    image: service_monitor:latest
    container_name: service_monitor_main
    restart: always
    ports:
      - "1280:3000"
    environment:
      PORT: 3000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
