version: '3.8'

services:
  docker_service_monitor:
    build: .
    container_name: main
    restart: always
    ports:
      - "1280:3000"
    environment:
      - PORT=3000
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock